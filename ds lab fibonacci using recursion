
#include <stdio.h>
int fact(int)
int main() {
    int n,z;
    printf("enter the no ");
    scanf("%d",&n);
    z=fact(n);
    printf("%d",z);
    return 0;
}
int fact(int n) {
    if(n==1){
        return 1;
         }
    else{
        return (n*fact(n-1));
    }
}
